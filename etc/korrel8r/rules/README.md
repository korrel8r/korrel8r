# Korrel8r Rules

This directory contains YAML configuration files that define additional correlation rules
loaded at runtime. Include new files in `all.yaml` to get them picked up by default configurations.

For rule syntax and examples, see
[Writing Rules](../../../doc/content/docs/writing-rules.md)
