statusRules:
  - name: HasFinalizer
    start:
      domain: k8s
    status: |-
      {{- with .metadata.finalizers}}has-finalizer{{end}}
  - name: PodPhase
    start:
      domain: k8s
      classes: [Pod]
    status: |-
      phase-{{.status.phase}}
