# Quick Rules (Compiled Rules)

See [doc.go](doc.go) for the full guide to writing, compiling, and testing compiled rules.

> **Edit only `*.qtpl`.** The `*.qtpl.go` and `applyfuncs.go` files are generated.
> Run `make generate` after editing.

For configuration-file rules (YAML with Go `text/template`), see
[Writing Rules](https://korrel8r.github.io/korrel8r/docs/writing-rules/).
