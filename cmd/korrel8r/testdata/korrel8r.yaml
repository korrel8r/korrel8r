# Empty configuration for korrel8r
stores:
  - domain: mock
rules:
  - name: foobar
    start:
      domain: mock
      classes: [foo]
    goal:
      domain: mock
    result:
      query: |-
        {"class": "mock.bar","results":["z"]}
