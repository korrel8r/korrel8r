---
# NOTE this has been modified for the Alertmanager API.
name: AlertToDeployment
start: [alert, alert]
goal: [k8s, Deployment.apps]
query: |-
  /api/v1/namespaces/{{.Alert.Labels.namespace}}/deployments/{{.Alert.Labels.deployment}}
